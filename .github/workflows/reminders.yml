name: Bot Reminders Schedule

on:
  schedule:
    # 03:00 UTC -> 6:00 AM Amman (Fajr)
    - cron: '0 3 * * *'
    # 05:00 UTC -> 8:00 AM Amman (Morning)
    - cron: '0 5 * * *'
    # 15:00 UTC -> 6:00 PM Amman (Evening)
    - cron: '0 15 * * *'
    # 21:00 UTC -> 12:00 AM Amman (Midnight)
    - cron: '0 21 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  ping-endpoints:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Dispatcher
        run: |
          echo "🚀 Triggering Central Dispatcher..."
          curl -s -X GET "https://cne-athkar-bot.vercel.app/api/cron/dispatch"
