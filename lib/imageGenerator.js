// Image generator DISABLED - canvas library not compatible with Vercel
// This is a stub file to prevent import errors

const generateAthkarImage = async (text, title = 'CNE Athkar') => {
    throw new Error('Image generation is temporarily disabled');
};

module.exports = { generateAthkarImage };
